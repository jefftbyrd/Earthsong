const config = {
  theme: {
    extend: {
      fontFamily: {
        basteleur: ['var(--font-basteleur)'],
      },
    },
  },
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
export default config;
